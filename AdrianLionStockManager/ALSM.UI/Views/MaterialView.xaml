<UserControl x:Class="ALSM.UI.Views.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DataGrid AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Name="Materials"
                      IsSynchronizedWithCurrentItem="True"
                      SelectedItem="{Binding SelectedMaterial}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityInStock}"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate}"/>
                    <DataGridTextColumn Header="Modified" Binding="{Binding LastModified}"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBox Text="{Binding SelectedMaterial.Description}"/>
            <Button x:Name="AddNewMaterial" Content="Add new Material"/>
            <Button x:Name="SaveChanges" Content="Save"/>
        </StackPanel>
    </Grid>
</UserControl>
